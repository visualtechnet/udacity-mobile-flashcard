const drawerOptions = () => {  
  const drawer = Object.assign({}, {
    drawerTintColor: '#F9FDCE',
    drawerTitleStyle: {
      fontWeight: 'bold'
    }
    
  })

  return drawer
}

export { drawerOptions }